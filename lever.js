class Lever{
  constructor(x,str1,str2){
    push()
    this.ler = createSprite(x,365,50,50)
    this.ler.addAnimation(str1,lr2)
    this.ler.addAnimation(str2,lr1)
    this.ler.scale = 0.18
    pop()
  }
}